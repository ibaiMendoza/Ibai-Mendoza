package Controlador;

import Modelo.EquipoDAO;
import Modelo.Jornada;
import Modelo.JornadaDAO;

import static Modelo.EquipoDAO.listaEquipos;

public class JornadaController {
    public JornadaController(JornadaDAO jornadaDAO) {

        public static void generarJornadas(){
            for(int i=0; i< listaEquipos.size()-1;i++){
                Jornada jornada = new Jornada();
                for(int j=0; j<listaEquipos.size()/2;j++){
                    EnfrentamientoController.
                }
            }
        }
    }
}